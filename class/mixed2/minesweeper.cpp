#include <iostream>
#include <array>
#include <vector>
#include <algorithm>
#include <stack>

using namespace std;
#define ll long long
#define mod 1000000007

bool cmp(pair<int,bool> p1, pair<int,bool> p2) {
    return false;
}

int N, M, K;
int main() {
    cin >> N >> M >> K;

    //for each
    // try mine
    // if any 9 are negative exclude
    // after a row if prev row not zeroed exclude
    // everything has to be zeroed at end
    // k mines have to be placed

    
}